sudo apt-get update && sudo apt-get upgrade -y

sudo apt-get update

sudo apt-get install build-essential libssl-dev

cd ~
# Change 0.33.5 <---- You see this the version may have changed check the nvm page
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.36.0/install.sh | bash
source ~/.profile #Use this in jenkins source ~/.nvm/nvm.sh
nvm install 6.0.0 #install based on your developer os
nvm use 6.0.0
node -v
# Outputs a number: "v6.0.0"
npm -v
# outputs a number "3.8.6"

